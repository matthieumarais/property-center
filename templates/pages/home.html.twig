{% extends 'base.html.twig' %}

{% block title 'Agence Lorem Ipsum' %}

{% block body %}
<div class='container'>
    <div class='jumbotron text-center'>
        <h1>Bienvenue sur la homepage</h1>
        <p>Lorem ipsum dolor sit, amet consectetur adipisicing elit. Iste, distinctio, tempora recusandae facilis esse nesciunt ad natus modi unde illo molestias cumque labore quo molestiae aspernatur pariatur! Officiis, distinctio vel.</p>
    </div>

    <div class="container">
        <h2>Les derniers biens</h2>
        <div class="row flex">
            {% for property in properties %}
                <div class="col-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <a href="{{ path('property.show', {id : property.id, slug : property.slug}) }}">{{ property.title }}</a>
                            </h5>
                            <p class="card-text">{{ property.city }} {{ property.postalCode }}</p>
                            <div class="text-danger">{{ property.formatedPrice }}</div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</div>

{% endblock %}